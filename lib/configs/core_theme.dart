import 'package:flutter/material.dart';

final themeLight = ThemeData(
  colorScheme: ColorScheme.light(
    primary: Color(0xffF48B0B),
    primaryContainer: Color(0xff032146)
  )

);

final themeDark = themeLight;
